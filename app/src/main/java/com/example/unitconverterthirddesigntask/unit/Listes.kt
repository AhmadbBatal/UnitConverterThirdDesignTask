package com.example.unitconverterthirddesigntask.unit

import com.example.unitconverterthirddesigntask.enumclasses.*

val timeList = listOf(
    TimeEnum.WEEK.name,
    TimeEnum.DAY.name,
    TimeEnum.MIN.name,
    TimeEnum.HOUR.name,
    TimeEnum.SECOND.name
)

val lengthList = listOf(
    LengthEnum.KILOMETER.name,
    LengthEnum.METER.name,
    LengthEnum.CENTIMETER.name,
    LengthEnum.MILE.name,
    LengthEnum.YARD.name
)

val temperatureList = listOf(
    TemperatureEnum.CELSIUS.name,
    TemperatureEnum.KELVIN.name,
    TemperatureEnum.FAHRENHEIT.name
)

val weightList = listOf(
    WeightEnum.KILOGRAM.name,
    WeightEnum.GRAM.name,
    WeightEnum.MILLIGRAM.name,
    WeightEnum.POUND.name,
)

val areaList = listOf(
    AreaEnum.SQUARE_METER.name,
    AreaEnum.SQUARE_KILOMETER.name,
    AreaEnum.SQUARE_MILE.name,
    AreaEnum.SQUARE_YARD.name,
    AreaEnum.SQUARE_FOOT.name
)